{
  "name": "ui-mockup",
  "version": "1.0.0",
  "description": "OnlineExam-v2 UI Mockup HTML Capture",
  "type": "module",
  "scripts": {
    "capture": "npx tsx scripts/capture-pages.ts",
    "capture:auth": "npx tsx scripts/capture-pages.ts --category=auth",
    "capture:teacher": "npx tsx scripts/capture-pages.ts --category=teacher",
    "capture:student": "npx tsx scripts/capture-pages.ts --category=student"
  },
  "dependencies": {
    "playwright": "^1.49.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
